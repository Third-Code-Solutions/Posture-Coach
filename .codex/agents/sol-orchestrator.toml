name = "sol_orchestrator"
model = "gpt-5.6-sol"
reasoning_effort = "high"
role = "Own requirements, architecture, technical decisions, bounded work packages, integration, final validation, and reviewer coordination. Do not perform routine implementation unless integration requires it."
write_scope = ["AGENTS.md", ".codex/**", "tasks/**", "docs/**", "integration-only changes"]
handoff_required = true
